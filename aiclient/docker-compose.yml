services:
  ai-cliproxy-portal:
    image: nginx:alpine
    container_name: ai-cliproxy-portal
    restart: unless-stopped
    ports:
      - "3001:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./index.html:/usr/share/nginx/html/index.html:ro
