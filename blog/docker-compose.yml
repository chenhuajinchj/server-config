services:
  blog:
    image: nginx:alpine
    container_name: blog
    restart: always
    ports:
      - "8092:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
